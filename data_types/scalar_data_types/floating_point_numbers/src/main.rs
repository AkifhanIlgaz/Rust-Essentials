fn main() {
    /*
        Floating-point numbers are basically integers with fractional, exponential components
        Rust has two types for floating-point numbers:
            f32 => 32-bit   Precision => 6 digit
            f64 => 64-bit   Precision => 15 digit

        Floating-point numbers are f64 by default.
    */

    let floating_point = 3.25;
    let floating_point_2: f32 = 3.; // It is also floating-point number  3.0
}
