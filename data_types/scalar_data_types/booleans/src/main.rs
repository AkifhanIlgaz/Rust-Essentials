fn main() {
    /*
        Like in the most programming languages booleans can have two possible values:

            true
            false

        Booleans are one byte in size

        We use "bool" keyword the data type of booleans
    */

    let boolean_true = true;
    let boolean_false: bool = false;
}
