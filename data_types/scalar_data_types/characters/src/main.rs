fn main() {
    /*
       The Character Type || char

       char type is four bytes in size and represents Unicode scalar value

       We use single quotes '' with char literals.

    */

    let c = 'z';
    let z: char = 'ℤ'; // with explicit type annotation
    let heart_eyed_cat = '😻';
}
